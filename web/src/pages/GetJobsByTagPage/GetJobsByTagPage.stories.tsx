import type { Meta, StoryObj } from '@storybook/react'

import GetJobsByTagPage from './GetJobsByTagPage'

const meta: Meta<typeof GetJobsByTagPage> = {
  component: GetJobsByTagPage,
}

export default meta

type Story = StoryObj<typeof GetJobsByTagPage>

export const Primary: Story = {}
